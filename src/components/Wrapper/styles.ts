import styled from 'styled-components'
import { colors } from '../../constants'

export const Wrapper = styled.div`
  background: linear-gradient(100deg, ${colors.black}, ${colors.whiteBlue});
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
`
